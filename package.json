{
  "name": "owwk-backup-hub",
  "version": "1.0.0",
  "description": "Backup & Mirror Hub for organization",
  "type": "module",
  "scripts": {
    "sync": "node sync.mjs",
    "deploy": "wrangler deploy --config worker/wrangler.toml"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "fs-extra": "^11.2.0",
    "tar": "^7.5.22"
  }
}
